build:
 emilianotorres123:  # cambiar por su namespcode
   image: okteto.dev/emilianotorres123:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all thace de okteto
dev:
 emilianotorres123:
   command: bash
   sync:
   - .:/teams  # cambiar por su proyecto

